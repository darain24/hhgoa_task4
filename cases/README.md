# Verified submission outputs

This directory is intentionally not populated with draft or mock answers. Run `uv run python scripts/export_submission.py` only after TigerGraph graph/vector grounding and persistence are verified for all twenty cases. Current drafts live in `output/draft-cases/`.
